#include <stdio.h>

int main(void) {
    int in;


    while(true)
    {
        scanf("%d", in); 
        int ar[20] 

        if(in < 0)
        {
            break;
        }


    }

   return 0;
}
